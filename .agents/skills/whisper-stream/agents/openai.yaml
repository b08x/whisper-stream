interface:
  display_name: "Whisper Stream"
  short_description: "Repo-specific patterns and workflows for whisper-stream"
  default_prompt: "Use the whisper-stream repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true